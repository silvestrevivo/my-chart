// Text strings for localization
interface LocalizationStrings {
  unlock: string;
  lock: string;
  lockButtonText: string;
}

const TEXT: LocalizationStrings = {
  unlock: "unlock",
  lock: "lock",
  lockButtonText: "Click on each dot to lock them individually",
};

export const L = TEXT;
